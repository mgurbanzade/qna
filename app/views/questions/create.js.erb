<% if @question.errors.present? %>
  $('.errors').html("<%= j render 'shared/errors', resource: @question %>");
<% else %>
  $('.errors').html('');
  $('.form-control').val('');
  $('.new-question').slideToggle();
<% end %>
