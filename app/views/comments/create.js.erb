<% if @comment.errors.present? %>
  $('.errors').html("<%= j render 'shared/errors', resource: @comment %>");
<% else %>
  $('.errors').html('');
  $('textarea.form-control').each(function() {
    $(this).val('');
  });
<% end %>
